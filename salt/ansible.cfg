[defaults]
inventory             = inventory.yml
roles_path            = roles
# auto_silent suppresses the Python interpreter discovery warning
interpreter_python    = auto_silent
# opt in to ansible-core 2.24 behaviour now — use ansible_facts["key"] syntax
inject_facts_as_vars  = false
# suppress deprecation noise; warnings are addressed in code
deprecation_warnings  = false
