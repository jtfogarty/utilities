---
# meta file for certs role
galaxy_info:
  role_name: certs
  description: >
    Installs mkcert, creates a local CA, and generates the TLS cert + key
    for nginx → salt-api HTTPS termination on Ubuntu.
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble

dependencies: []
